import makeSx from "@utils/makeSx";

export default makeSx({
  header: {
    display: {
      base: "flex",
      md: "none",
    },
    padding: {
      base: "1rem",
      lg: "1rem 3rem",
    },
  },
});
