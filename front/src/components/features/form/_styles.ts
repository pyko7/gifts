import makeSx from "../../../utils/makeSx";

export default makeSx({
  button: {
    width: "100%",
  },
  inputGroup: {
    display: "flex",
    flexDirection: "column",
    alignItems: "flex-end",
  },
});
